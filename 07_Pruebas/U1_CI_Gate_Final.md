# U1 — CI GATE FINAL

U1 queda preparado para integración mediante Pull Request.

El workflow CI del repositorio se activa en `pull_request` y valida pytest y los esquemas SQL existentes.

**Regla:** sin SUCCESS no hay merge a `main`.
