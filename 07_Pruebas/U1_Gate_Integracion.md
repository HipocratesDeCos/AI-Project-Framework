# U1 — GATE DE INTEGRACIÓN

**Estado:** CERRADO EN RAMA — PENDIENTE CI DE PR

La materialización de U1 está completada en `design/u1-ceo-frontend`.

El workflow existente se activa en `pull_request` y `main`, ejecutando pytest y las validaciones SQL del repositorio.

El merge a `main` queda condicionado a CI SUCCESS.
