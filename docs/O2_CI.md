# EIOS — O2 CI

La ejecución CI de la rama `feat/o2-coordinated-decision-support` es requisito de integración.

Workflow esperado: `EIOS Tests`.

Criterio: todos los tests y validaciones existentes, más `tests/test_o2.py`, deben finalizar en SUCCESS. Un fallo impide el merge.
